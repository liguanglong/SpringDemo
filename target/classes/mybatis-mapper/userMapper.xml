<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.UserDao">

    <select id="getAllUsersCount" resultType="int">
        select count(*) from user
    </select>

    <select id="findUserById" resultType="com.example.pojo.User" parameterType="int">
        select * from user where id=#{value}
    </select>

    <select id="judgeByUserNameAndPassWord" resultType="int" >
        SELECT COUNT(*) from user WHERE name = #{username} and password=#{password}
    </select>

    <select id="getUserByName" resultType="com.example.pojo.User" parameterType="String">
        select * from user WHERE name = #{value}
    </select>

    <resultMap id="userResultMap" type="User">
        <id property="id" column="id"></id>
        <result property="name" column="name"></result>
        <result property="password" column="password"></result>
        <result property="email" column="email"></result>
        <result property="lastLogin" column="last_login"></result>
    </resultMap>

    </mapper>